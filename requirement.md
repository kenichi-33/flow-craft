# 申請フォーム作成機能付きワークフローシステム 要件定義

## 1. 目的・背景

本システムは、業務申請における **申請フォームの設計** と **承認フローの設計・実行** を GUI で一元的に行えるワークフロー基盤を提供することを目的とする。
従来のレガシーなワークフローシステムでは、画面変更やフロー変更に開発工数がかかる課題があるため、本システムでは **ノーコード／ローコード** による柔軟な業務変更を可能とする。

---

## 2. システム全体構成（概要）

* フロントエンドとバックエンドを分離した Web アプリケーション
* フォーム定義・フロー定義をデータとして管理し、実行時に画面・フローを動的生成
* ローカル開発環境では docker-compose により一式を起動可能

---

## 3. フロントエンド要件

### 3.1 画面構成

フロントエンドは以下の3系統の画面で構成する。

#### ① 開発画面（設計者向け）

**申請フォーム作成画面**

* ドラッグ＆ドロップによるフォーム設計
* 対応コンポーネント

  * テキストボックス
  * テキストエリア
  * 数値入力
  * 日付／日時
  * プルダウン（マスタ参照可）
  * チェックボックス／ラジオボタン
* 項目設定

  * ラベル名
  * 必須／任意
  * バリデーション（文字数、数値範囲等）
  * 初期値
* レイアウト

  * グリッド／自由配置
  * 行・列構成
* フォーム定義の保存・読み込み

**フローエディタ**

* ノードベースの GUI フロー設計
* 対応ノード

  * 開始
  * 申請
  * 承認
  * 差戻し
  * 分岐（条件式）
  * 終了
* 設定項目

  * 承認者（ユーザー／ロール）
  * 条件分岐ルール
  * 承認期限
* フロー定義の保存・バージョン管理

---

#### ② 実行画面（利用者向け）

**申請画面**

* 設計されたフォーム定義から動的に生成
* 入力補助・リアルタイムバリデーション
* 一時保存／申請実行

**申請一覧画面**

* 自分の申請一覧
* 承認待ち一覧
* ステータス表示（下書き／申請中／承認済／差戻し等）
* フィルタ・検索

---

#### ③ 管理画面（管理者向け）

**目的**

* ワークフロー全体の可視化
* 滞留・遅延・未対応タスクの把握
* 運用時の例外対応

**主な機能**

**フロー進捗可視化**

* 申請単位の進捗ステータス表示
* 現在のフローノード（承認段階）表示
* フロー図上での進捗ハイライト表示

**TODO／タスク管理**

* 承認待ちタスク一覧
* ユーザー別／ロール別 TODO
* 滞留時間表示（SLA超過検知）
* 優先度・期限順ソート

**申請管理機能**

* 申請データ検索・参照
* 強制承認／差戻し／中断
* 代理承認設定

**モニタリング機能**

* フロー別処理件数
* 承認平均時間
* 滞留ノードランキング

**運用補助**

* 操作ログ・監査ログ確認
* APIコンポーネント実行結果確認

---

## 4. バックエンド要件

本システムのバックエンドは **UI/API 層** と **Workflow Engine 層** を論理的に分離し、
将来的な Queue / Worker 化に耐えられる構成とする。

---

### 4.1 Engine の責務定義（APIとの境界）

#### 4.1.1 Backend API（UI向け）の責務

* フロントエンド向け API 提供
* 認証・認可（RBAC）
* フォーム定義・フロー定義 CRUD
* 申請作成・参照リクエスト受付
* 管理画面用の検索・集計 API

**禁止事項**

* フロー状態遷移ロジックを持たない
* タスク実行判断を行わない

---

#### 4.1.2 Workflow Engine の責務

* ワークフロー実行の唯一の制御主体
* Workflow / Task の状態管理
* 状態遷移の永続化（RDB）
* タスク生成・完了判定
* リトライ・失敗制御
* 実行イベント発行

**特徴**

* UI・認証・表示ロジックを持たない
* 決定論的（deterministic）な状態遷移
* Queue / Worker に置き換え可能な設計

---

### 4.2 Workflow / Task ステータス定義（状態遷移表）

#### 4.2.1 Workflow（申請インスタンス）ステータス

| ステータス     | 説明   | 遷移元               | 遷移先                          |
| --------- | ---- | ----------------- | ---------------------------- |
| CREATED   | 生成直後 | -                 | RUNNING                      |
| RUNNING   | 実行中  | CREATED / WAITING | WAITING / FAILED / COMPLETED |
| WAITING   | 承認待ち | RUNNING           | RUNNING / FAILED             |
| FAILED    | 失敗   | RUNNING / WAITING | RETRYING / SUSPENDED         |
| RETRYING  | 再試行中 | FAILED            | RUNNING / FAILED             |
| SUSPENDED | 中断   | FAILED / RUNNING  | RUNNING                      |
| COMPLETED | 完了   | RUNNING           | -                            |

---

#### 4.2.2 Task（承認・API 等）ステータス

| ステータス    | 説明   | 遷移元     | 遷移先                |
| -------- | ---- | ------- | ------------------ |
| PENDING  | 実行待ち | -       | RUNNING            |
| RUNNING  | 実行中  | PENDING | SUCCESS / FAILED   |
| SUCCESS  | 正常終了 | RUNNING | -                  |
| FAILED   | 失敗   | RUNNING | RETRYING / SKIPPED |
| RETRYING | 再試行  | FAILED  | RUNNING / FAILED   |
| SKIPPED  | スキップ | FAILED  | -                  |

---

### 4.3 フロー処理機能（Engine前提）

* フロー定義に基づくノード解決
* Workflow / Task の状態遷移制御
* TODO（承認タスク）生成・完了
* APIコンポーネント実行制御
* 状態変更イベント発行

---

## 5. 機能要件

### 5.1 申請検索・参照機能（曖昧検索対応）

**要件**

* 申請内容（入力値）を対象とした検索
* 部分一致・あいまい検索に対応
* ステータス・申請者・期間などとの複合検索

**対象データ**

* 申請タイトル
* 申請フォーム入力値（JSON）
* コメント・差戻し理由

**検索仕様**

* キーワード検索（AND / OR）
* 部分一致・類似検索
* フィルタ（状態・申請者・日付）

---

### 5.2 ワークフローAPIコンポーネント機能

**概要**
ワークフロー内に外部／内部 API を呼び出す「APIコンポーネント（ノード）」を組み込み、
申請処理の自動化・外部連携を可能とする。

**対応ユースケース**

* 申請内容の外部システム登録
* マスタ情報のリアルタイム取得
* 条件判定用データ取得

**APIコンポーネント仕様**

* HTTP メソッド：GET / POST / PUT
* エンドポイントURL設定
* ヘッダ設定（認証情報含む）
* リクエストマッピング（申請データ → API）
* レスポンスマッピング（API → ワークフロー変数）

**エラー制御**

* リトライ回数設定
* エラー時分岐（成功／失敗）

---

## 6. 非機能要件

### 6.1 性能

* 通常操作において 3 秒以内に画面表示
* 同時利用者数：100〜300人規模を想定

### 6.2 可用性

* 業務時間帯の安定稼働
* フロー実行中データの整合性保証

### 6.3 セキュリティ

* 認証・認可（RBAC）
* API レベルでのアクセス制御
* 操作ログの取得

### 6.4 保守性・拡張性

* フロント／バックエンド分離
* 定義データ駆動設計
* マイクロサービス化を将来検討可能な構成

---

## 7. アーキテクチャ構成（Workflow Engine 前提）

### 7.1 推奨アーキテクチャ全体像

```
[ Browser ]
     |
     v
[ Frontend (SPA) ]
     |
     v
[ Backend API ]  --- UI向け / 認証 / CRUD
     |
     v
[ Workflow Engine ]  --- 状態遷移 / 実行制御
     |
     +--> [ PostgreSQL ]  --- 状態・履歴（正）
     |
     +--> [ Queue ] ※将来
     |
     +--> [ Worker ] ※将来
```

---

### 7.2 状態管理と非同期化の考え方

* 状態遷移は必ず Engine + RDB
* Queue は副作用処理専用
* 再実行可能・冪等性前提

---

### 7.1 推奨アーキテクチャ（全体像）

**構成方針**

* フロントエンド / バックエンドを明確に分離
* バックエンドは「API + ドメインロジック集中型」から開始
* フォーム定義・フロー定義を **メタデータ駆動** で管理
* 将来は認証・通知・フロー実行を分離可能な設計とする

```
[ Browser ]
     |
     v
[ Frontend (SPA) ]  --- 静的配信 / API呼び出し
     |
     v
[ Backend API ]  --- 認証 / 業務ロジック / フロー制御
     |
     +--> [ RDB (PostgreSQL) ]  --- 定義・申請・履歴
     |
     +--> [ Cache (Redis) ] ※将来
     |
     +--> [ Auth Server (Keycloak) ] ※将来
```

---

### 7.2 フロントエンド構成（推奨）

**役割**

* フォームビルダー / フローエディタの GUI 提供
* 定義データをもとにした動的画面生成
* API との疎結合通信

**構成例**

* React + TypeScript
* 状態管理：Redux Toolkit / Zustand
* UI：MUI / Ant Design / 独自コンポーネント
* フローエディタ：React Flow 等

**画面単位の責務分離**

* /designer/forms ：フォーム設計
* /designer/flows ：フロー設計
* /app/apply ：申請実行
* /app/tasks ：承認タスク
* /admin ：管理

---

### 7.3 バックエンド構成（推奨）

**基本方針**

* 初期は **モノリス構成 + レイヤード設計**
* 将来マイクロサービス化しやすい境界を意識

**構成例（Node.js）**

* フレームワーク：NestJS（推奨） / Express

**レイヤ構成**

```
Controller（API層）
 └ Application Service（ユースケース）
     └ Domain（申請・フロー・定義モデル）
         └ Infrastructure（DB / 外部I/O）
```

**主要ドメイン**

* FormDefinition
* FlowDefinition
* Application（申請）
* ApprovalTask
* ApprovalHistory

---

### 7.4 データベース構成（推奨）

#### 基本構成（必須）

**RDB：PostgreSQL**

* 申請・承認は強い整合性が必要
* 状態遷移・履歴管理に最適

**設計方針**

* フォーム定義：JSONB
* フロー定義：JSONB
* 申請データ：JSONB + メタ情報正規化
* 履歴・状態：正規化テーブル

---

#### 検索向け拡張構成（推奨）

**Option A：PostgreSQL フルテキスト検索（初期）**

* JSONB → 検索用カラムへ正規化／抽出
* tsvector による全文検索
* 小〜中規模向け

**Option B：Elasticsearch / OpenSearch（拡張）**

* 申請検索専用インデックス
* あいまい検索・類義語・スコアリング対応
* 大量データ・高速検索向け

```
[ PostgreSQL ]  --- 正データ（整合性）
      |
      +--> [ Search Index (Elastic) ] --- 検索特化
```

---

#### データ同期方針

* 申請作成・更新時に検索インデックス更新
* 非同期処理（将来）で再構築可能

---

### 7.5 認証・認可（段階導入）

**Phase 1（初期）**

* アプリ内ユーザー管理
* ロールベースアクセス制御（RBAC）

**Phase 2（拡張）**

* Keycloak 連携
* SSO / 外部IdP

---

### 7.6 非同期・将来拡張構成（Queue 移行前提）

**段階的アーキテクチャ方針**

**Phase 1：同期処理（初期）**

* API リクエスト内で状態遷移・タスク生成を完結
* 副作用処理はインプロセスイベントとして実行

```
[ Backend API ]
   └─ 状態遷移 → イベント → ハンドラ実行
```

---

**Phase 2：非同期処理（拡張）**

* 状態遷移後にイベントを Queue に送信
* Worker が副作用処理を担当

```
[ Backend API ]
   └─ 状態遷移 → Event Publish
                         ↓
                      [ Queue ]
                         ↓
                      [ Worker ] → API / 通知 / 検索
```

---

**Queue 技術候補**

* Redis + BullMQ（シンプル・Node.js 向け）
* RabbitMQ（堅牢・将来の多言語対応）
* Kafka（大規模・イベント駆動強化時）

---

**実装上の注意点**

* 状態遷移は必ず RDB トランザクション内で完結
* Queue 処理は冪等性を担保
* 再実行・リトライ設計を前提とする

---

### 7.7 docker-compose 構成（推奨）

**構成サービス**

* frontend
* backend
* postgres

```
services:
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"

  backend:
    build: ./backend
    ports:
      - "8080:8080"
    depends_on:
      - postgres

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: workflow
      POSTGRES_USER: workflow
      POSTGRES_PASSWORD: workflow
```

---

## 8. ローカル開発環境（docker-compose）

### 8.1 構成

* frontend コンテナ
* backend コンテナ
* database コンテナ

### 8.2 要件

* docker-compose up で即時起動
* ホットリロード対応
* 初期データ投入用 seed

---

## 9. 今後の拡張想定

* 外部システム連携（Webhook / API）
* 承認通知（メール／Slack）
* フロー条件の高度化
* マルチテナント対応

---

## 追加方針：申請フォーム × ワークフローを**1つのアプリケーションとして統合**する構成

### 基本方針

本システムは、**申請フォーム機能とワークフロー機能を論理的に分離しつつ、ユーザー視点では1つのアプリケーションとして提供**する構成とする。

* UI・認証・権限・申請体験は**単一アプリケーション**
* 内部的には **Workflow Engine をサブコンポーネントとして内包**
* フォーム定義とフロー定義は**同一ドメインモデル上で関連付け**

---

### 推奨アーキテクチャ（統合型）

```
[ Frontend (SPA) ]
   ├─ 申請フォームビルダー
   ├─ フローエディタ
   ├─ 申請実行画面
   └─ 管理ダッシュボード
          ↓ REST / GraphQL
[ Application Backend ]  ← 単一アプリ
   ├─ Form Domain
   │    ├─ フォーム定義管理
   │    └─ 入力バリデーション
   ├─ Workflow Domain
   │    ├─ フロー定義
   │    ├─ ステータス管理
   │    └─ TODO生成
   ├─ Workflow Engine Module（内包）
   │    ├─ 状態遷移制御
   │    ├─ Task 実行（人/API）
   │    └─ Retry / Timeout / 分岐
   ├─ Search Module
   │    └─ 曖昧検索 / 全文 / ベクトル
   └─ Integration Module
        └─ 外部API / Webhook
          ↓
[ PostgreSQL ]  [ OpenSearch ]  [ Queue（将来） ]
```

---

### 統合ドメイン設計の要点

#### フォームとワークフローの関係

* FormDefinition

  * workflowId を必須で保持
* WorkflowDefinition

  * entryFormId を参照

➡ **申請 = フォーム入力 + ワークフロー開始** を原子的に扱う

---

### Engine を内包する理由

* ユーザー・管理者は「ワークフローエンジン」を意識しない
* デプロイ・運用を単純化
* 将来、Engine 部分のみを

  * マイクロサービス化
  * Kestra / Temporal 置き換え
    できるように、**内部API境界を明示**

---

### API 境界（内部）

* UI → Application API（公開）
* Application → Engine API（内部）

例：

* POST /applications/{id}/submit
  → Engine.startWorkflow()

---

### 将来拡張（分離可能性）

| 項目       | 初期       | 将来                     |
| -------- | -------- | ---------------------- |
| Engine   | 内包モジュール  | 独立サービス                 |
| 状態管理     | DB       | Queue / Event Driven   |
| 検索       | RDB LIKE | OpenSearch / Vector DB |
| API Task | 同期       | 非同期・再実行                |

---

### docker-compose 前提

* frontend
* backend（App + Engine）
* postgres
* opensearch

※ Queue（Kafka / SQS）は feature flag で後付け可能

---

### 本構成の位置づけ

* Camunda の embedded engine
* Kestra を内包した業務アプリ
* "フォーム駆動型ワークフローSaaS" の最小かつ拡張可能構成

---
